# common/calamares/modules/shellprocess.conf
---
dontChroot: false
timeout: 600
name: "Finalizing Duff-Linux System"
script:
    # --- 1. Installer Cleanup ---
    - "xbps-remove -Fy calamares"
    - "rm -f /etc/skel/.config/autostart/calamares.desktop"
    - "rm -rf /usr/share/calamares"
    - "rm -f /home/*/.config/autostart/calamares.desktop"

    # --- 2. Btrfs & GRUB Stability Fixes ---
    - "find /boot -maxdepth 1 -type f -exec cp -p --sparse=never {} {}.tmp \\; -exec mv -f {}.tmp {} \\;"
    - "echo 'GRUB_PRELOAD_MODULES=\"btrfs zstd\"' >> /etc/default/grub"
    - "sed -i 's/GRUB_CMDLINE_LINUX_DEFAULT=\"/GRUB_CMDLINE_LINUX_DEFAULT=\"rootflags=subvol=@ /' /etc/default/grub"
    - "grep -q '^GRUB_DISABLE_OS_PROBER=' /etc/default/grub || echo 'GRUB_DISABLE_OS_PROBER=true' >> /etc/default/grub"
    
    # --- 3. Dependency Check ---
    - "xbps-install -Sy snapper cronie grub-btrfs dbus"

    # --- 4. The DBus Chroot Workaround ---
    - "mkdir -p /run/dbus"
    - "dbus-uuidgen --ensure"
    - "dbus-daemon --system --fork --print-pid > /run/chroot-dbus.pid"

    # --- 5. Snapper Configuration & Baseline ---
    - "umount /.snapshots"
    - "rm -rf /.snapshots"
    - "snapper -c root create-config /"
    - "btrfs subvolume delete /.snapshots"
    - "mkdir /.snapshots"
    - "mount /.snapshots"
    - "chmod a+rx /.snapshots"
    - "snapper -c root create --description 'Baseline Installation' --cleanup-algorithm number"
    
    # --- 6. Wire Core Services into Runit Stage 1 ---
    # Void Linux's /etc/runit/1 does not source core-services by default.
    # Use awk to insert the hook before the runsvdir line (avoids sed syntax issues).
    - |
        if ! grep -q 'run-core-services' /etc/runit/1; then
            awk '/runsvdir/ && !p {print ". /etc/runit/run-core-services.sh"; p=1} {print}' \
                /etc/runit/1 > /tmp/runit1.new && \
            mv /tmp/runit1.new /etc/runit/1 && \
            chmod 755 /etc/runit/1
        fi

    # --- 7. Install the Parser-Proof XBPS Wrapper ---
    - "mkdir -p /usr/local/bin"
    - |
        cat <<'EOF' | sed 's/^        //' > /usr/local/bin/xbps-install
        #!/bin/sh
        REAL=/usr/bin/xbps-install

        should_manage_snapshots() {
            command -v snapper >/dev/null 2>&1 || return 1
            command -v findmnt >/dev/null 2>&1 || return 1
            [ -d /.snapshots ] || return 1

            ROOT_FSTYPE=$(findmnt -n -o FSTYPE / 2>/dev/null)
            ROOT_SUBVOL=$(findmnt -n -o OPTIONS / 2>/dev/null | tr ',' '\n' | sed -n 's/^subvol=//p' | head -n 1)

            case "$ROOT_FSTYPE:$ROOT_SUBVOL" in
                btrfs:/@|btrfs:@) return 0 ;;
            esac

            return 1
        }

        should_snapshot=0
        explicit_root=0
        skip_next=0

        for arg in "$@"; do
            if [ "$skip_next" -eq 1 ]; then
                skip_next=0
                continue
            fi

            case "$arg" in
                -h|--help)
                    exec "$REAL" "$@"
                    ;;
                -r|--rootdir|-c|--cachedir|-C|--config|-R|--repository)
                    [ "$arg" = "-r" ] || [ "$arg" = "--rootdir" ] && explicit_root=1
                    skip_next=1
                    ;;
                --rootdir=*)
                    explicit_root=1
                    ;;
                --cachedir=*|--config=*|--repository=*)
                    ;;
                -*)
                    case "$arg" in
                        *u*|*U*) should_snapshot=1 ;;
                    esac
                    ;;
                *)
                    should_snapshot=1
                    ;;
            esac
        done

        if [ "$explicit_root" -eq 1 ] || [ "$should_snapshot" -eq 0 ] || ! should_manage_snapshots; then
            exec "$REAL" "$@"
        fi

        snapper -c root create --description "Before XBPS: $*" --cleanup-algorithm number
        "$REAL" "$@"
        status=$?

        if [ "$status" -eq 0 ]; then
            grub-mkconfig -o /boot/grub/grub.cfg > /dev/null 2>&1
        fi

        exit "$status"
        EOF
    - "chmod +x /usr/local/bin/xbps-install"
    - |
        cat <<'EOF' | sed 's/^        //' > /usr/local/bin/xbps-remove
        #!/bin/sh
        REAL=/usr/bin/xbps-remove

        should_manage_snapshots() {
            command -v snapper >/dev/null 2>&1 || return 1
            command -v findmnt >/dev/null 2>&1 || return 1
            [ -d /.snapshots ] || return 1

            ROOT_FSTYPE=$(findmnt -n -o FSTYPE / 2>/dev/null)
            ROOT_SUBVOL=$(findmnt -n -o OPTIONS / 2>/dev/null | tr ',' '\n' | sed -n 's/^subvol=//p' | head -n 1)

            case "$ROOT_FSTYPE:$ROOT_SUBVOL" in
                btrfs:/@|btrfs:@) return 0 ;;
            esac

            return 1
        }

        should_snapshot=0
        explicit_root=0
        skip_next=0

        for arg in "$@"; do
            if [ "$skip_next" -eq 1 ]; then
                skip_next=0
                continue
            fi

            case "$arg" in
                -h|--help)
                    exec "$REAL" "$@"
                    ;;
                -r|--rootdir|-C|--config)
                    [ "$arg" = "-r" ] || [ "$arg" = "--rootdir" ] && explicit_root=1
                    skip_next=1
                    ;;
                --rootdir=*)
                    explicit_root=1
                    ;;
                --config=*)
                    ;;
                -*)
                    ;;
                *)
                    should_snapshot=1
                    ;;
            esac
        done

        if [ "$explicit_root" -eq 1 ] || [ "$should_snapshot" -eq 0 ] || ! should_manage_snapshots; then
            exec "$REAL" "$@"
        fi

        snapper -c root create --description "Before XBPS Remove: $*" --cleanup-algorithm number
        "$REAL" "$@"
        status=$?

        if [ "$status" -eq 0 ]; then
            grub-mkconfig -o /boot/grub/grub.cfg > /dev/null 2>&1
        fi

        exit "$status"
        EOF
    - "chmod +x /usr/local/bin/xbps-remove"

    # --- 8. OctoXBPS Notifier Sudo Fix (For Terminal) ---
    - "echo '%wheel ALL=(ALL) NOPASSWD: /usr/bin/xbps-install -S, /usr/bin/xbps-install -Sy, /usr/bin/xbps-install -Syy, /usr/local/bin/xbps-install -S, /usr/local/bin/xbps-install -Sy, /usr/local/bin/xbps-install -Syy' > /etc/sudoers.d/10-xbps-sync"
    - "chmod 440 /etc/sudoers.d/10-xbps-sync"

    # --- 9. OctoXBPS Notifier PolicyKit Fix (For GUI Applets) ---
    - "mkdir -p /etc/polkit-1/rules.d"
    - |
        printf 'polkit.addRule(function(action, subject) {
            if (action.id == "org.freedesktop.policykit.exec" &&
                action.lookup("program") == "/usr/local/bin/xbps-install" &&
                subject.isInGroup("wheel")) {
                return polkit.Result.YES;
            }
        });\n' > /etc/polkit-1/rules.d/10-xbps-sync.rules

    # --- 10. Cleanup DBus & Finalize Bootloader ---
    - "kill $(cat /run/chroot-dbus.pid)"
    - "rm /run/chroot-dbus.pid"
    - "grub-mkconfig -o /boot/grub/grub.cfg"

    # --- 11. Duff Linux Identity ---
    - |
        printf 'NAME="Duff Linux"\nID=duff-linux\nID_LIKE=void\nPRETTY_NAME="Duff Linux"\nANSI_COLOR="1;33"\nHOME_URL="https://github.com/duffnshmrt/duff-linux"\nSUPPORT_URL="https://github.com/duffnshmrt/duff-linux"\nBUG_REPORT_URL="https://github.com/duffnshmrt/duff-linux/issues"\nLOGO=duff-linux\n' > /etc/os-release

    # --- 12. Fastfetch on terminal open (for users already created by Calamares) ---
    - |
        find /home /root -maxdepth 2 -name .bashrc | \
            xargs -r grep -L fastfetch 2>/dev/null | \
            xargs -r -I FNAME sh -c 'printf "\n[ -t 1 ] && fastfetch\n" >> FNAME'

    # --- 13. KDE Taskbar Start Button Icon ---
    - "mkdir -p /usr/share/icons/hicolor/256x256/apps"
    - "cp /usr/share/pixmaps/duff-linux.png /usr/share/icons/hicolor/256x256/apps/duff-linux.png"

    # --- 14. Zram Swap ---
    - "ln -sf /etc/sv/zramen /etc/runit/runsvdir/default/zramen"
